name: CI

on: pull_request

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - uses: UziTech/action-setup-atom@v1.0.2
    - name: Test Hydrogen
      run: apm test
